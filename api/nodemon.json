{
  "watch": ["src", "../src"],
  "ext": "ts",
  "ignore": ["src/**/*.test.ts", "dist", "node_modules"],
  "exec": "pnpm run build && serverless offline start",
  "env": {
    "NODE_ENV": "development"
  },
  "delay": 1000
}
